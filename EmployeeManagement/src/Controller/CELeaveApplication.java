
package Controller;

import Model.*;
public class CELeaveApplication {
     public void checkApp(int id,String mydob,String mydob2,String reason)
    {
        MELeaveApplication mc =new MELeaveApplication();
        mc.checkApp(id,mydob,mydob2,reason);
    }
}
